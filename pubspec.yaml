name: my_flutter_app
description: A minimal Flutter application scaffolded by Copilot.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  excel: ^2.1.0
  path_provider: ^2.0.15
  intl: ^0.18.1
  shared_preferences: ^2.2.2
  flutter_typeahead: ^5.2.0
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
